{
  "name": "noise-www",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "The Noise language website + in-browser playground (Astro + Monaco + WASM).",
  "scripts": {
    "predev": "pnpm --filter @noiselang/core run build",
    "prebuild": "pnpm --filter @noiselang/core run build",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.14",
    "@noiselang/core": "workspace:*",
    "astro": "^5.13.0",
    "monaco-editor": "^0.52.2"
  }
}
